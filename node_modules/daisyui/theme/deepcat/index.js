import deepcat from './object.js';
import {
    addPrefix
} from '../../functions/addPrefix.js';

export default ({
    addBase,
    prefix = ''
}) => {
    const prefixeddim = addPrefix(deepcat, prefix);
    addBase({
        ...prefixeddim
    });
};
